{
  "name": "quizzifypro",
  "version": "1.0.0",
  "description": "Interactive Quiz and Survey Web App",
  "main": "server/server.js",
  "scripts": {
    "start": "cd client && npm start",
    "server": "cd server && nodemon server.js",
    "dev": "concurrently \"npm run server\" \"npm start --prefix client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.11",
    "cors": "^2.8.5",
    "jsonwebtoken": "^8.5.1",
    "bcrypt": "^5.0.1"
    // Add other backend dependencies as needed
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "concurrently": "^7.0.0"
    // Add other development dependencies as needed
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "private": true,
  "workspaces": [
    "client",
    "server"
  ]
}
